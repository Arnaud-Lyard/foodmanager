export const JWT_ACCESS_TOKEN = {
  PRIVATE_KEY: "accessTokenPrivateKey",
  PUBLIC_KEY: "accessTokenPublicKey",
} as const;

export const JWT_REFRESH_TOKEN = {
  PRIVATE_KEY: "refreshTokenPrivateKey",
  PUBLIC_KEY: "refreshTokenPublicKey",
} as const;
